import React from 'react';
import '../styles/GlobalHeader.css';

const GlobalHeader = () => {
    return (
        <header className='global-header'>
            <img src='/icons/NinjaOneLogo.svg' alt='Logo' className='logo' />
        </header>
    );
};

export default GlobalHeader;
